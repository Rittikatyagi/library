name: Docker CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend Image
        run: docker build -t riiiitiiiika/django-backend:latest -f Dockerfile.backend .

      - name: Push Backend Image
        run: docker push riiiitiiiika/django-backend:latest

      - name: Deploy to Render
        run: curl "https://api.render.com/deploy/srv-d7opp0osfn5c73fnphgg?key=8u4cYKxhdJE"